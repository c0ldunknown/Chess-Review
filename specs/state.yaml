# Chess Review — State
# Active session tracking

workflow_mode: solo-git

session:
  active_flow: null
  active_epic_id: null
  active_story_id: null

git:
  branch: main
  head: 5b9016a
  clean: true

handoff:
  next_skill: kickoff-branch
  notes: "Plans written for e01 (natural language blunder analysis). 3 stories: proxy+panel, jump-to-error, config toggle. Ready to build."

metrics:
  story_start: null